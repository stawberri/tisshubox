#tisshubox
  display flex
  flex-flow column
  height 100vh
  width 100vw
  transition-property background
  transition-duration 0.69s
  $vertical-duration = (@transition-duration * 0.69)
  overflow hidden

  @import tisshuframe
  @import tisshu
  @import pagebar
  @import buttons
