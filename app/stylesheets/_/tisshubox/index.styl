#tisshubox
  display flex
  flex-flow column
  height 100vh
  width 100vw
  transition-property background
  transition-duration 0.69s
  $vertical-duration = (@transition-duration * 0.69)
  overflow hidden

  @import update
  @import tisshuframe
  @import pagebar
  @import buttons
